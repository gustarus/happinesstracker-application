'use strict';

import {AppRegistry} from 'react-native';

// import library constructor
const HappinessTracker = require('./application');

// register library component
AppRegistry.registerComponent('happinesstracker', () => HappinessTracker);
